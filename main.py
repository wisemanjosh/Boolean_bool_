# Declare a variable is_raining and set it to True if it's currently raining in London or False if it's not.
# Create a boolean variable is_sunny and set it to True if it's sunny.

is_raining = False  # Change to true if it's raining

is_sunny = True  # Change to false if it's not sunny
